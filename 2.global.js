// __dirname = path of the current file
// __filename = file name
// require = function to use the modules
// module = info of the current module
// process = info of the env where the program is executed

console.log(__dirname);
console.log(__filename);
console.log(module);

// remember the module.exports function we will use this in future